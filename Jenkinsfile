pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Hello Build started'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying'
            }
        }
    }
}
